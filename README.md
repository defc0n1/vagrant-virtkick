# Automatically provision a virtual VirtKick host
 
    cd ~/src
    git clone https://github.com/heichblatt/vagrant-virtkick.git
    cd vagrant-virtkick
    vagrant up
    xdg-open http://localhost:3000/
